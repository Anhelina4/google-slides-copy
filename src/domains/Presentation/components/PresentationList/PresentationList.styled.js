import styled from 'styled-components'

const PresentationListWrapper = styled.div`
  padding: 16px;
  width: var(--presentation-list-width);
  margin-bottom: 8px;
  margin-top: 8px;
`

export default PresentationListWrapper
